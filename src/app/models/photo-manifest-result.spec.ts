import { PhotoManifestResult } from './photo-manifest-result';

describe('PhotoManifestResult', () => {
  it('should create an instance', () => {
    expect(new PhotoManifestResult()).toBeTruthy();
  });
});
